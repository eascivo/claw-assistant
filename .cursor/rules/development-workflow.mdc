---
description: 开发流程——测试先行、本地跑通、功能拆分、保持简洁
alwaysApply: true
---

# 开发流程

## 0. 自主规划，逐步达成

- **自主规划**：根据整体目标自行拆解任务、排定顺序，不依赖逐条指令才动。
- **整体目标**：始终对齐最终目标（如 [SYSTEM-DESIGN.md](docs/SYSTEM-DESIGN.md) 中的阶段与交付物），每步都服务于该目标。
- **逐步完成**：按规划顺序一小步一小步完成，每步可验证、可交付，避免一次铺开过大。

## 1. 测试先行，再集成

- **每增加一个功能**：先写该功能的**单元测试**，跑通并验证通过后，再将该功能集成进主流程。
- **集成后**：做**全体集成测试**并验证结果与目标是否一致；若不一致，必须修改逻辑直至通过，不得跳过。

## 2. 本地优先，再扩展

- **先按本地跑通整个流程**：确保端到端在本地可运行、可验证。
- **流程跑通后再**：逐步集成新功能，避免在流程未通时堆砌功能。

## 3. 功能拆分，小步开发

- **先拆功能**：按功能维度拆分需求，拆小、拆到可独立验证的粒度。
- **按功能维度开发**：一个功能一个功能完成，可先搭框架，再逐步完成各模块。
- **保持简洁**：及时清理无用代码与废弃逻辑，不保留「可能以后用」的死代码。

## 4. 完成即推送

- **每完成一个功能**（单元测试与集成验证通过后）：提交并**推送到远程仓库**，保证远程与本地进度一致，便于协作与回溯。
