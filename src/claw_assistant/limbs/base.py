"""Limb 接口（Phase 1 用 callable 即可，此处占位）。"""

from typing import Any


def limb_interface(params: dict[str, Any]) -> dict[str, Any]:
    """占位：正式 Limb 可实现此签名。"""
    return {"ok": True, "message": "stub"}
